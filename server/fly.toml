# Fly.io: `fly launch --copy-config --no-deploy` в папке server/Teach.Api, затем
# fly volumes create teach_data --size 1
# fly secrets set ANTHROPIC_API_KEY=... Teach__ApiToken=...
# fly deploy
app = "teach-api"
primary_region = "ams"

[build]
  dockerfile = "Teach.Api/Dockerfile"

[env]
  Teach__Db = "Data Source=/data/teach.db"
  Teach__Model = "auto"

[[mounts]]
  source = "teach_data"
  destination = "/data"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "off"
  auto_start_machines = true
  min_machines_running = 1

[[http_service.checks]]
  path = "/health"
  interval = "30s"
  timeout = "5s"
